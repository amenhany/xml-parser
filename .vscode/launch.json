{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run xml_editor",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/xml_editor",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "CMake Build" // Build before running (Optional)
        }
    ]
}